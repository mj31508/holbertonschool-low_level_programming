#include "3-calc.h"
#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[])
{
	int total = 0;
	int a = 1;
	int b = 1;
	int c = 1;
	int d = 1;

	if (argc != 4)
	{
		printf("Error\n");
		exit (98);

	}

	if (

	if (argv == "/" || argv == "%")
	{
		printf ("Error\n");
		exit (98);
	}


	total = atoi(argv[a] + argv[b] + argv[c] + argv[d]);
	printf("%d", total);

}
