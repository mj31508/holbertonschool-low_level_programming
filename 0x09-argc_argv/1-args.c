#include <stdio.h>

/**
 * main - funtion that holds the argument count
 * @argc: counts the total num of arguments
 * @argv: amount of arguments supplied
 * Return: 0
 ***/

int main(int argc, char __attribute__ ((unused)) *argv[])
{


		printf("%d\n", argc - 1);
		return (0);
}
