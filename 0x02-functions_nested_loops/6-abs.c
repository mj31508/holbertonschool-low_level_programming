#include "holberton.h"
#include <stdio.h>

/**
 *_abs - computing the absoulute value of a integer
 *@int - the parameter being passed
 *@i: int argument that is being passed
 *Return: Always 0.
 */

int _abs(int i)
{
	if (i < 0)
		i = i * -1;
		return (i);
}
