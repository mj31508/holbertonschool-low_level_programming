#include <stdio.h>
int main()
{
	int a = 0;
	int b = 0;

	add(a, b)
	{
		return (a+b);
	}

	sub(a,b)
	{
	return(a-b);
	}

	mul(a,b)
	{
		return(a*b);
	}

	div(a,b)
	{
		return(a/b);
	}

	mod(a,b)
	{
		return(a%b);
	}
}
